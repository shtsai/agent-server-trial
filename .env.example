ANTHROPIC_API_KEY=sk-ant-...
# Local only. On both platforms this is injected: a Vercel binding sets it, and on Railway it is
# http://agent.railway.internal:3001. Never set it by hand on either.
AGENT_SERVICE_URL=http://localhost:3001
PORT=3001
